import 'package:flutter/material.dart';

class MenuAkun extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: new Text("MENU AKUN")
      )
    );
  }
}
